/* Auto-generated configuration file - DO NOT EDIT MANUALLY */
import GLib from 'gi://GLib';

export const Config = {
    PACKAGE_NAME: '@PACKAGE_NAME@',
    VERSION: '@PACKAGE_VERSION@',
    WEBSITE: 'https://gitlab.com/valo27/valot',
    DEVELOPER: 'Vitaly Odnoyko',
    APPLICATION_ID: '@APPLICATION_ID@',
    RESOURCE_PATH: '@resource_path@',

    getValotReportsDir() {
        // Always use Documents/valot (localized Documents directory)
        const documentsDir = GLib.get_user_special_dir(GLib.UserDirectory.DIRECTORY_DOCUMENTS);
        if (documentsDir) {
            return GLib.build_filenamev([documentsDir, 'valot']);
        }
        // Fallback to home Documents/valot if special dir not available
        return GLib.build_filenamev([GLib.get_home_dir(), 'Documents', 'valot']);
    },

    getDocumentsDir() {
        // Get localized Documents directory (e.g., "Dokumente" in German)
        const documentsDir = GLib.get_user_special_dir(GLib.UserDirectory.DIRECTORY_DOCUMENTS);
        if (documentsDir) {
            return documentsDir;
        }
        // Fallback to hardcoded Documents if special dir not available
        return GLib.build_filenamev([GLib.get_home_dir(), 'Documents']);
    }
};
